[install]
exact = true
auto = "disable"
minimumReleaseAge = 259200

[run]
shell = "bun"
bun = false
noOrphans = true

[test]
# Scratch work in tmp can hold test files that are not part of the suite.
pathIgnorePatterns = ["tmp/**"]
preload = ["./tests/harness/preload.ts"]
