diff --git a/out/node/persistd/readiness.js b/out/node/persistd/readiness.js
new file mode 100644
index 0000000..7b11822
--- /dev/null
+++ b/out/node/persistd/readiness.js
@@ -0,0 +1,103 @@
+"use strict";
+var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
+    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
+    return new (P || (P = Promise))(function (resolve, reject) {
+        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
+        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
+        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
+        step((generator = generator.apply(thisArg, _arguments || [])).next());
+    });
+};
+Object.defineProperty(exports, "__esModule", { value: true });
+exports.agentboxStartingPage = agentboxStartingPage;
+exports.checkPersistdReadiness = checkPersistdReadiness;
+const fs_1 = require("fs");
+const persistdRunDir = "/run/persistd";
+const persistdDataDir = "/data/persistd";
+const readyPath = `${persistdRunDir}/ready`;
+const restoreFailedPath = `${persistdRunDir}/restore-failed`;
+const watchFailedPath = `${persistdRunDir}/watch-failed`;
+const restoreErrorPath = `${persistdDataDir}/restore-error.log`;
+const watchErrorPath = `${persistdDataDir}/watch-error.log`;
+function checkPersistdReadiness() {
+    return __awaiter(this, void 0, void 0, function* () {
+        if (yield exists(restoreFailedPath)) {
+            return {
+                ready: false,
+                message: yield failureMessage("persistd restore failed", restoreErrorPath),
+            };
+        }
+        if (yield exists(watchFailedPath)) {
+            return {
+                ready: false,
+                message: yield failureMessage("persistd watch failed", watchErrorPath),
+            };
+        }
+        let data;
+        try {
+            data = yield fs_1.promises.readFile(readyPath, "utf8");
+        }
+        catch (error) {
+            if ((error === null || error === void 0 ? void 0 : error.code) === "ENOENT") {
+                return { ready: false, message: "persistd is starting" };
+            }
+            return { ready: false, message: "persistd ready file cannot be read" };
+        }
+        let parsed;
+        try {
+            parsed = JSON.parse(data);
+        }
+        catch (_a) {
+            return { ready: false, message: "persistd ready file is invalid" };
+        }
+        if (parsed.ready !== true) {
+            return { ready: false, message: "persistd is starting" };
+        }
+        if (typeof parsed.updatedAt !== "string" || parsed.updatedAt.length === 0) {
+            return { ready: false, message: "persistd ready file is invalid" };
+        }
+        return {
+            ready: true,
+            message: "persistd is ready",
+            updatedAt: parsed.updatedAt,
+        };
+    });
+}
+function agentboxStartingPage() {
+    return `<style>body{font-family:monospace;white-space:pre}</style>Agentbox is starting
+<script>
+async function waitUntilReady() {
+  try {
+    if ((await fetch("/healthz", { cache: "no-store" })).ok) {
+      location.reload();
+      return;
+    }
+  } catch {}
+  setTimeout(waitUntilReady, 1000);
+}
+waitUntilReady();
+</script>
+`;
+}
+function exists(path) {
+    return __awaiter(this, void 0, void 0, function* () {
+        try {
+            yield fs_1.promises.stat(path);
+            return true;
+        }
+        catch (_a) {
+            return false;
+        }
+    });
+}
+function failureMessage(fallback, path) {
+    return __awaiter(this, void 0, void 0, function* () {
+        try {
+            const firstLine = (yield fs_1.promises.readFile(path, "utf8")).split(/\r?\n/, 1)[0];
+            return firstLine || fallback;
+        }
+        catch (_a) {
+            return fallback;
+        }
+    });
+}
diff --git a/out/node/routes/health.js b/out/node/routes/health.js
index 7f9a04b..16761c6 100644
--- a/out/node/routes/health.js
+++ b/out/node/routes/health.js
@@ -1,4 +1,4 @@
-"use strict";
+"use strict";
 var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
     function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
     return new (P || (P = Promise))(function (resolve, reject) {
@@ -12,13 +12,16 @@ Object.defineProperty(exports, "__esModule", { value: true });
 exports.wsRouter = exports.router = void 0;
 const express_1 = require("express");
 const wsRouter_1 = require("../wsRouter");
+const readiness_1 = require("../persistd/readiness");
 exports.router = (0, express_1.Router)();
-exports.router.get("/", (req, res) => {
-    res.json({
+exports.router.get("/", (req, res) => __awaiter(void 0, void 0, void 0, function* () {
+    const persistd = yield (0, readiness_1.checkPersistdReadiness)();
+    res.status(persistd.ready ? 200 : 503).json({
         status: req.heart.alive() ? "alive" : "expired",
         lastHeartbeat: req.heart.lastHeartbeat,
+        persistd,
     });
-});
+}));
 exports.wsRouter = (0, wsRouter_1.Router)();
 exports.wsRouter.ws("/", (req) => __awaiter(void 0, void 0, void 0, function* () {
     wsRouter_1.wss.handleUpgrade(req, req.ws, req.head, (ws) => {
@@ -32,4 +35,4 @@ exports.wsRouter.ws("/", (req) => __awaiter(void 0, void 0, void 0, function* ()
         req.ws.resume();
     });
 }));
-//# sourceMappingURL=health.js.map
\ No newline at end of file
+//# sourceMappingURL=health.js.map
diff --git a/out/node/routes/index.js b/out/node/routes/index.js
index 60484f4..5256f74 100644
--- a/out/node/routes/index.js
+++ b/out/node/routes/index.js
@@ -1,4 +1,4 @@
-"use strict";
+"use strict";
 var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
     if (k2 === undefined) k2 = k;
     var desc = Object.getOwnPropertyDescriptor(m, k);
@@ -54,6 +54,7 @@ const path = __importStar(require("path"));
 const http_1 = require("../../common/http");
 const util_1 = require("../../common/util");
 const cli_1 = require("../cli");
+const readiness_1 = require("../persistd/readiness");
 const constants_1 = require("../constants");
 const heart_1 = require("../heart");
 const http_2 = require("../http");
@@ -135,6 +136,30 @@ const register = (app, args) => __awaiter(void 0, void 0, void 0, function* () {
         res.set("Content-Type", (0, util_2.getMediaMime)(resourcePath));
         res.send(yield fs_1.promises.readFile(resourcePath));
     }));
+    const persistdGate = (req, res, next) => __awaiter(void 0, void 0, void 0, function* () {
+        if (/^\/healthz\/?$/.test(req.path)) {
+            return next();
+        }
+        const persistd = yield (0, readiness_1.checkPersistdReadiness)();
+        if (persistd.ready) {
+            return next();
+        }
+        if (req.headers.accept && req.headers.accept.includes("text/html")) {
+            res.setHeader("Cache-Control", "no-store");
+            res.setHeader("Retry-After", "1");
+            return res.status(503).type("html").send((0, readiness_1.agentboxStartingPage)());
+        }
+        return res.status(503).type("text").send("Agentbox is starting\n");
+    });
+    app.router.use(persistdGate);
+    app.wsRouter.use((req, _, next) => __awaiter(void 0, void 0, void 0, function* () {
+        const persistd = yield (0, readiness_1.checkPersistdReadiness)();
+        if (persistd.ready) {
+            return next();
+        }
+        req._ws_handled = true;
+        req.ws.end("HTTP/1.1 503 Service Unavailable\r\nConnection: close\r\n\r\n");
+    }));
     app.router.use("/", domainProxy.router);
     app.wsRouter.use("/", domainProxy.wsRouter.router);
     app.router.all("/proxy/:port{/*path}", (req, res) => __awaiter(void 0, void 0, void 0, function* () {
@@ -204,4 +229,4 @@ const register = (app, args) => __awaiter(void 0, void 0, void 0, function* () {
     };
 });
 exports.register = register;
-//# sourceMappingURL=index.js.map
\ No newline at end of file
+//# sourceMappingURL=index.js.map
