# Optional code-server settings. Compose loads this file into the container
# (env_file). Leave it empty to register the password in the browser on first
# visit. See docs/self-hosting.md for every supported variable.

# PASSWORD=example
# HASHED_PASSWORD='$argon2id$v=19$m=...'
