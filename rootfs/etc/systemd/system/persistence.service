[Unit]
Description=Composery filesystem persistence daemon
After=local-fs.target
Before=composery.service

[Service]
Type=simple
ExecStart=/opt/persistence/bin/persistence daemon
Restart=always
RestartSec=1

[Install]
WantedBy=multi-user.target
