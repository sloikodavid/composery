name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-26.04
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          submodules: true
      - uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5.0.0
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "24.14.1"
          cache: pnpm
      - run: sudo apt-get update && sudo apt-get install -y --no-install-recommends git-lfs quilt
      # Cheaper early gate than the full image build: lay our VS Code-side
      # patch stack over pristine code-server (submodule) and quilt-push with
      # fuzz=0, exactly like packages/ide/build.sh does at build time. Our own
      # src/node changes are overlay files (not patches), so this only exercises
      # the lib/vscode patch stack.
      - name: Verify IDE patch stack applies
        working-directory: packages/ide
        env:
          QUILT_PUSH_ARGS: "--fuzz=0"
        run: |
          while IFS= read -r p; do
            [ -z "$p" ] || { cp "patches/$p" "upstream/patches/$p"; printf '%s\n' "$p" >> upstream/patches/series; }
          done < patches/series
          cd upstream && QUILT_PATCHES=patches quilt push -a
      - uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: packages/cli
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
