diff --git a/lib/vscode/src/vs/platform/terminal/common/terminal.ts b/lib/vscode/src/vs/platform/terminal/common/terminal.ts
index 66c95ed..9de9962 100644
--- a/lib/vscode/src/vs/platform/terminal/common/terminal.ts
+++ b/lib/vscode/src/vs/platform/terminal/common/terminal.ts
@@ -339,6 +339,10 @@ export interface IPtyService {
 	listProcesses(includeAttached?: boolean): Promise<IProcessDetails[]>;
 	registerDataConsumer(id: number, consumerId: string): Promise<void>;
 	unregisterDataConsumer(id: number, consumerId: string): Promise<void>;
+	registerTerminalViewport(id: number, viewportId: string): Promise<void>;
+	unregisterTerminalViewport(id: number, viewportId: string): Promise<void>;
+	resizeTerminalViewport(id: number, viewportId: string, cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): Promise<void>;
+	activateTerminalViewport(id: number, viewportId: string, cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): Promise<void>;
 	getPerformanceMarks(): Promise<performance.PerformanceMark[]>;
 	/**
 	 * Measures and returns the latency of the current and all other processes to the pty host.
@@ -832,6 +836,7 @@ export interface ITerminalChildProcess {
 	sendSignal(signal: string): void;
 	processBinary(data: string): Promise<void>;
 	resize(cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): void;
+	activateViewport?(cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): void;
 	clearBuffer(): void | Promise<void>;
 
 	/**
diff --git a/lib/vscode/src/vs/platform/terminal/node/ptyHostService.ts b/lib/vscode/src/vs/platform/terminal/node/ptyHostService.ts
index afec94d..3e53a8c 100644
--- a/lib/vscode/src/vs/platform/terminal/node/ptyHostService.ts
+++ b/lib/vscode/src/vs/platform/terminal/node/ptyHostService.ts
@@ -238,6 +238,18 @@ export class PtyHostService extends Disposable implements IPtyHostService {
 	unregisterDataConsumer(id: number, consumerId: string): Promise<void> {
 		return this._proxy.unregisterDataConsumer(id, consumerId);
 	}
+	registerTerminalViewport(id: number, viewportId: string): Promise<void> {
+		return this._proxy.registerTerminalViewport(id, viewportId);
+	}
+	unregisterTerminalViewport(id: number, viewportId: string): Promise<void> {
+		return this._proxy.unregisterTerminalViewport(id, viewportId);
+	}
+	resizeTerminalViewport(id: number, viewportId: string, cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): Promise<void> {
+		return this._proxy.resizeTerminalViewport(id, viewportId, cols, rows, pixelWidth, pixelHeight);
+	}
+	activateTerminalViewport(id: number, viewportId: string, cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): Promise<void> {
+		return this._proxy.activateTerminalViewport(id, viewportId, cols, rows, pixelWidth, pixelHeight);
+	}
 	async getPerformanceMarks(): Promise<performance.PerformanceMark[]> {
 		return this._optionalProxy?.getPerformanceMarks() ?? [];
 	}
diff --git a/lib/vscode/src/vs/platform/terminal/node/ptyService.ts b/lib/vscode/src/vs/platform/terminal/node/ptyService.ts
index 4c62085..67abed2 100644
--- a/lib/vscode/src/vs/platform/terminal/node/ptyService.ts
+++ b/lib/vscode/src/vs/platform/terminal/node/ptyService.ts
@@ -420,6 +420,26 @@ export class PtyService extends Disposable implements IPtyService {
 		this._ptys.get(id)?.unregisterDataConsumer(consumerId);
 	}
 
+	@traceRpc
+	async registerTerminalViewport(id: number, viewportId: string): Promise<void> {
+		this._throwIfNoPty(id).registerViewport(viewportId);
+	}
+
+	@traceRpc
+	async unregisterTerminalViewport(id: number, viewportId: string): Promise<void> {
+		this._ptys.get(id)?.unregisterViewport(viewportId);
+	}
+
+	@traceRpc
+	async resizeTerminalViewport(id: number, viewportId: string, cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): Promise<void> {
+		this._throwIfNoPty(id).resizeViewport(viewportId, cols, rows, pixelWidth, pixelHeight);
+	}
+
+	@traceRpc
+	async activateTerminalViewport(id: number, viewportId: string, cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): Promise<void> {
+		this._throwIfNoPty(id).activateViewport(viewportId, cols, rows, pixelWidth, pixelHeight);
+	}
+
 	@traceRpc
 	async updateTitle(id: number, title: string, titleSource: TitleEventSource): Promise<void> {
 		this._throwIfNoPty(id).setTitle(title, titleSource);
@@ -732,10 +752,21 @@ const enum InteractionState {
 	Session = 'Session'
 }
 
+interface IViewportDimensions {
+	cols: number;
+	rows: number;
+	pixelWidth?: number;
+	pixelHeight?: number;
+}
+
 class PersistentTerminalProcess extends Disposable {
 
 	private readonly _bufferer: TerminalDataBufferer;
 	private readonly _dataFlowControl: TerminalDataFlowControl;
+	private readonly _viewports = new Map<string, { dimensions?: IViewportDimensions; activation: number }>();
+	private _activeViewport: string | undefined;
+	private _viewportActivation = 0;
+	private _dimensions: IViewportDimensions;
 
 	private readonly _pendingCommands = new Map<number, { resolve: (data: unknown) => void; reject: (err: unknown) => void }>();
 
@@ -829,6 +860,7 @@ class PersistentTerminalProcess extends Disposable {
 		fixedDimensions?: IFixedTerminalDimensions
 	) {
 		super();
+		this._dimensions = { cols, rows };
 		this._dataFlowControl = new TerminalDataFlowControl(charCount => this._terminalProcess.acknowledgeDataEvent(charCount));
 		this._interactionState = new MutationLogger(`Persistent process "${this._persistentProcessId}" interaction state`, InteractionState.None, this._logService);
 		this._wasRevived = reviveBuffer !== undefined;
@@ -907,6 +939,53 @@ class PersistentTerminalProcess extends Disposable {
 		this._dataFlowControl.unregister(consumerId);
 	}
 
+	registerViewport(viewportId: string): void {
+		if (!this._viewports.has(viewportId)) {
+			this._viewports.set(viewportId, { activation: 0 });
+		}
+	}
+
+	unregisterViewport(viewportId: string): void {
+		if (!this._viewports.delete(viewportId) || this._activeViewport !== viewportId) {
+			return;
+		}
+		this._activeViewport = undefined;
+		let next: { id: string; dimensions: IViewportDimensions; activation: number } | undefined;
+		for (const [id, viewport] of this._viewports) {
+			if (viewport.dimensions && viewport.activation > 0 && (!next || viewport.activation > next.activation)) {
+				next = { id, dimensions: viewport.dimensions, activation: viewport.activation };
+			}
+		}
+		if (next) {
+			this._activeViewport = next.id;
+			this._resize(next.dimensions);
+		}
+	}
+
+	resizeViewport(viewportId: string, cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): void {
+		const viewport = this._viewports.get(viewportId);
+		if (!viewport) {
+			throw new Error(`Terminal viewport "${viewportId}" is not registered`);
+		}
+		const dimensions = { cols, rows, pixelWidth, pixelHeight };
+		viewport.dimensions = dimensions;
+		if (this._activeViewport === viewportId) {
+			this._resize(dimensions);
+		}
+	}
+
+	activateViewport(viewportId: string, cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): void {
+		const viewport = this._viewports.get(viewportId);
+		if (!viewport) {
+			throw new Error(`Terminal viewport "${viewportId}" is not registered`);
+		}
+		const dimensions = { cols, rows, pixelWidth, pixelHeight };
+		viewport.dimensions = dimensions;
+		viewport.activation = ++this._viewportActivation;
+		this._activeViewport = viewportId;
+		this._resize(dimensions);
+	}
+
 	async refreshProperty<T extends ProcessPropertyType>(type: T): Promise<IProcessPropertyMap[T]> {
 		return this._terminalProcess.refreshProperty(type);
 	}
@@ -966,15 +1045,24 @@ class PersistentTerminalProcess extends Disposable {
 		return this._terminalProcess.processBinary(data);
 	}
 	resize(cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): void {
+		this._resize({ cols, rows, pixelWidth, pixelHeight });
+	}
+
+	private _resize(dimensions: IViewportDimensions): void {
 		if (this._inReplay) {
 			return;
 		}
-		this._serializer.handleResize(cols, rows);
+		if (this._dimensions.cols === dimensions.cols && this._dimensions.rows === dimensions.rows &&
+			this._dimensions.pixelWidth === dimensions.pixelWidth && this._dimensions.pixelHeight === dimensions.pixelHeight) {
+			return;
+		}
+		this._dimensions = dimensions;
+		this._serializer.handleResize(dimensions.cols, dimensions.rows);
 
 		// Buffered events should flush when a resize occurs
 		this._bufferer.flushBuffer(this._persistentProcessId);
 
-		return this._terminalProcess.resize(cols, rows, pixelWidth, pixelHeight);
+		return this._terminalProcess.resize(dimensions.cols, dimensions.rows, dimensions.pixelWidth, dimensions.pixelHeight);
 	}
 	async clearBuffer(): Promise<void> {
 		this._serializer.clearBuffer();
diff --git a/lib/vscode/src/vs/server/node/remoteTerminalChannel.ts b/lib/vscode/src/vs/server/node/remoteTerminalChannel.ts
index 4e42709..c543f23 100644
--- a/lib/vscode/src/vs/server/node/remoteTerminalChannel.ts
+++ b/lib/vscode/src/vs/server/node/remoteTerminalChannel.ts
@@ -36,12 +36,9 @@ import { promiseWithResolvers, raceTimeout } from '../../base/common/async.js';
 import { shouldUseEnvironmentVariableCollection } from '../../platform/terminal/common/terminalEnvironment.js';
 import { hasKey } from '../../base/common/types.js';
 
-interface IClientDimensions { cols: number; rows: number; pixelWidth?: number; pixelHeight?: number }
-
 export class TerminalClientState {
 	private readonly _clients = new Map<number, Set<string>>();
-	private readonly _controllers = new Map<number, string>();
-	private readonly _dimensions = new Map<number, Map<string, IClientDimensions>>();
+	private readonly _uiTargets = new Map<number, string>();
 	private readonly _acknowledged = new Map<number, number>();
 	private readonly _clientAcknowledged = new Map<number, Map<string, number>>();
 	private readonly _streaming = new Map<number, Set<string>>();
@@ -59,47 +56,46 @@ export class TerminalClientState {
 			this._clientAcknowledged.set(id, clientAcknowledged);
 		}
 		clientAcknowledged.set(clientId, this._acknowledged.get(id) ?? 0);
-		if (makeController || !this._controllers.has(id)) {
-			this._controllers.set(id, clientId);
+		if (makeController || !this._uiTargets.has(id)) {
+			this._uiTargets.set(id, clientId);
 		}
 		if (streaming) {
 			this.markStreaming(id, clientId);
 		}
 	}
 
-	detach(id: number, clientId: string): { attached: boolean; final: boolean; nextController?: string; dimensions?: IClientDimensions } {
+	detach(id: number, clientId: string): { attached: boolean; final: boolean; nextUiTarget?: string } {
 		const clients = this._clients.get(id);
 		if (!clients?.delete(clientId)) {
 			return { attached: false, final: false };
 		}
-		this._dimensions.get(id)?.delete(clientId);
 		this._clientAcknowledged.get(id)?.delete(clientId);
 		this._streaming.get(id)?.delete(clientId);
 		if (!clients.size) {
 			this.clear(id);
 			return { attached: true, final: true };
 		}
-		if (this._controllers.get(id) !== clientId) {
+		if (this._uiTargets.get(id) !== clientId) {
 			return { attached: true, final: false };
 		}
-		const nextController = clients.values().next().value as string;
-		this._controllers.set(id, nextController);
-		return { attached: true, final: false, nextController, dimensions: this._dimensions.get(id)?.get(nextController) };
+		const nextUiTarget = clients.values().next().value as string;
+		this._uiTargets.set(id, nextUiTarget);
+		return { attached: true, final: false, nextUiTarget };
 	}
 
-	detachClient(clientId: string): { id: number; final: boolean; dimensions?: IClientDimensions }[] {
-		const detached: { id: number; final: boolean; dimensions?: IClientDimensions }[] = [];
+	detachClient(clientId: string): { id: number; final: boolean }[] {
+		const detached: { id: number; final: boolean }[] = [];
 		for (const id of [...this._clients.keys()]) {
 			if (this._clients.get(id)?.has(clientId)) {
 				const result = this.detach(id, clientId);
-				detached.push({ id, final: result.final, dimensions: result.dimensions });
+				detached.push({ id, final: result.final });
 			}
 		}
 		return detached;
 	}
 
 	isAttached(id: number, clientId: string): boolean { return this._clients.get(id)?.has(clientId) ?? false; }
-	isController(id: number, clientId: string): boolean { return this._controllers.get(id) === clientId; }
+	isUiTarget(id: number, clientId: string): boolean { return this._uiTargets.get(id) === clientId; }
 	isStreaming(id: number, clientId: string): boolean { return this._streaming.get(id)?.has(clientId) ?? false; }
 
 	markStreaming(id: number, clientId: string): void {
@@ -114,25 +110,10 @@ export class TerminalClientState {
 		streamingClients.add(clientId);
 	}
 
-	recordDimensions(id: number, clientId: string, dimensions: IClientDimensions): boolean {
-		if (!this.isAttached(id, clientId)) {
-			return false;
-		}
-		let byClient = this._dimensions.get(id);
-		if (!byClient) {
-			byClient = new Map();
-			this._dimensions.set(id, byClient);
+	takeUiControl(id: number, clientId: string): void {
+		if (this.isAttached(id, clientId)) {
+			this._uiTargets.set(id, clientId);
 		}
-		byClient.set(clientId, dimensions);
-		return this.isController(id, clientId);
-	}
-
-	takeControl(id: number, clientId: string): IClientDimensions | undefined {
-		if (!this.isAttached(id, clientId) || this.isController(id, clientId)) {
-			return undefined;
-		}
-		this._controllers.set(id, clientId);
-		return this._dimensions.get(id)?.get(clientId);
 	}
 
 	acknowledge(id: number, clientId: string, charCount: number): number {
@@ -152,8 +133,7 @@ export class TerminalClientState {
 
 	clear(id: number): void {
 		this._clients.delete(id);
-		this._controllers.delete(id);
-		this._dimensions.delete(id);
+		this._uiTargets.delete(id);
 		this._acknowledged.delete(id);
 		this._clientAcknowledged.delete(id);
 		this._streaming.delete(id);
@@ -330,6 +310,7 @@ export class RemoteTerminalChannel extends Disposable implements IServerChannel<
 				this._addWorkspaceClient(createArgs.workspaceId, ctx.clientId);
 				const result = await this._createProcess(uriTransformer, createArgs);
 				this._clientState.attach(result.persistentTerminalId, ctx.clientId, true, true);
+				await this._ptyHostService.registerTerminalViewport(result.persistentTerminalId, ctx.clientId);
 				this._freshPtys.add(result.persistentTerminalId);
 				this._processWorkspaces.set(result.persistentTerminalId, { workspaceId: createArgs.workspaceId, persistent: createArgs.shouldPersistTerminal });
 				return result;
@@ -337,6 +318,7 @@ export class RemoteTerminalChannel extends Disposable implements IServerChannel<
 			case RemoteTerminalChannelRequest.AttachToProcess: {
 				await this._ptyHostService.attachToProcess(args[0]);
 				this._clientState.attach(args[0], ctx.clientId, false);
+				await this._ptyHostService.registerTerminalViewport(args[0], ctx.clientId);
 				return;
 			}
 			case RemoteTerminalChannelRequest.DetachFromProcess: {
@@ -344,9 +326,7 @@ export class RemoteTerminalChannel extends Disposable implements IServerChannel<
 				if (!detached.attached) {
 					return;
 				}
-				if (detached.dimensions) {
-					await this._ptyHostService.resize(args[0], detached.dimensions.cols, detached.dimensions.rows, detached.dimensions.pixelWidth, detached.dimensions.pixelHeight);
-				}
+				await this._ptyHostService.unregisterTerminalViewport(args[0], ctx.clientId);
 				if (detached.final) {
 					await this._ptyHostService.unregisterDataConsumer(args[0], 'vscode');
 					return this._ptyHostService.detachFromProcess(args[0], args[1]);
@@ -382,10 +362,7 @@ export class RemoteTerminalChannel extends Disposable implements IServerChannel<
 				if (!this._clientState.isAttached(args[0], ctx.clientId)) {
 					throw new Error(`Client "${ctx.clientId}" is not attached to terminal "${args[0]}"`);
 				}
-				const dimensions = this._clientState.takeControl(args[0], ctx.clientId);
-				if (dimensions) {
-					await this._ptyHostService.resize(args[0], dimensions.cols, dimensions.rows, dimensions.pixelWidth, dimensions.pixelHeight);
-				}
+				this._clientState.takeUiControl(args[0], ctx.clientId);
 				return this._ptyHostService.input(args[0], args[1]);
 			}
 			case RemoteTerminalChannelRequest.SendSignal: return this._ptyHostService.sendSignal.apply(this._ptyHostService, args);
@@ -395,8 +372,16 @@ export class RemoteTerminalChannel extends Disposable implements IServerChannel<
 			}
 			case RemoteTerminalChannelRequest.Shutdown: return this._ptyHostService.shutdown.apply(this._ptyHostService, args);
 			case RemoteTerminalChannelRequest.Resize: {
-				const dimensions = { cols: args[1], rows: args[2], pixelWidth: args[3], pixelHeight: args[4] };
-				return this._clientState.recordDimensions(args[0], ctx.clientId, dimensions) ? this._ptyHostService.resize(args[0], args[1], args[2], args[3], args[4]) : undefined;
+				if (!this._clientState.isAttached(args[0], ctx.clientId)) {
+					throw new Error(`Client "${ctx.clientId}" is not attached to terminal "${args[0]}"`);
+				}
+				return this._ptyHostService.resizeTerminalViewport(args[0], ctx.clientId, args[1], args[2], args[3], args[4]);
+			}
+			case RemoteTerminalChannelRequest.ActivateViewport: {
+				if (!this._clientState.isAttached(args[0], ctx.clientId)) {
+					throw new Error(`Client "${ctx.clientId}" is not attached to terminal "${args[0]}"`);
+				}
+				return this._ptyHostService.activateTerminalViewport(args[0], ctx.clientId, args[1], args[2], args[3], args[4]);
 			}
 			case RemoteTerminalChannelRequest.ClearBuffer: return this._ptyHostService.clearBuffer.apply(this._ptyHostService, args);
 			case RemoteTerminalChannelRequest.GetInitialCwd: return this._ptyHostService.getInitialCwd.apply(this._ptyHostService, args);
@@ -484,8 +469,8 @@ export class RemoteTerminalChannel extends Disposable implements IServerChannel<
 			case RemoteTerminalChannelEvent.OnProcessReadyEvent: return Event.filter(this._ptyHostService.onProcessReady, e => this._startState.target(e.id) === ctx.clientId) as Event<T>;
 			case RemoteTerminalChannelEvent.OnProcessExitEvent: return Event.filter(this._ptyHostService.onProcessExit, e => this._clientState.isAttached(e.id, ctx.clientId)) as Event<T>;
 			case RemoteTerminalChannelEvent.OnProcessReplayEvent: return Event.filter(this._ptyHostService.onProcessReplay, e => this._startState.target(e.id) === ctx.clientId) as Event<T>;
-			case RemoteTerminalChannelEvent.OnProcessOrphanQuestion: return Event.filter(this._ptyHostService.onProcessOrphanQuestion, e => this._clientState.isController(e.id, ctx.clientId)) as Event<T>;
-			case RemoteTerminalChannelEvent.OnExecuteCommand: return Event.filter(this.onExecuteCommand, e => this._clientState.isController(e.persistentProcessId, ctx.clientId)) as Event<T>;
+			case RemoteTerminalChannelEvent.OnProcessOrphanQuestion: return Event.filter(this._ptyHostService.onProcessOrphanQuestion, e => this._clientState.isUiTarget(e.id, ctx.clientId)) as Event<T>;
+			case RemoteTerminalChannelEvent.OnExecuteCommand: return Event.filter(this.onExecuteCommand, e => this._clientState.isUiTarget(e.persistentProcessId, ctx.clientId)) as Event<T>;
 			case RemoteTerminalChannelEvent.OnDidRequestDetach: return Event.filter(this._ptyHostService.onDidRequestDetach || Event.None, e => this._isWorkspaceLeader(e.workspaceId, ctx.clientId)) as Event<T>;
 			case RemoteTerminalChannelEvent.OnDidChangeProperty: return Event.filter(this._ptyHostService.onDidChangeProperty, e => this._startState.target(e.id) ? this._startState.target(e.id) === ctx.clientId : this._clientState.isAttached(e.id, ctx.clientId)) as Event<T>;
 			case RemoteTerminalChannelEvent.OnPersistentTerminalInventoryChange: return this._clientLifetimeEvent(ctx.clientId) as Event<T>;
@@ -509,9 +494,7 @@ export class RemoteTerminalChannel extends Disposable implements IServerChannel<
 					clients.delete(clientId);
 				}
 				for (const detached of this._clientState.detachClient(clientId)) {
-					if (detached.dimensions) {
-						void this._ptyHostService.resize(detached.id, detached.dimensions.cols, detached.dimensions.rows, detached.dimensions.pixelWidth, detached.dimensions.pixelHeight);
-					}
+					void this._ptyHostService.unregisterTerminalViewport(detached.id, clientId);
 					if (detached.final) {
 						void this._ptyHostService.unregisterDataConsumer(detached.id, 'vscode');
 						void this._ptyHostService.detachFromProcess(detached.id);
diff --git a/lib/vscode/src/vs/workbench/contrib/terminal/browser/agentHostPty.ts b/lib/vscode/src/vs/workbench/contrib/terminal/browser/agentHostPty.ts
index 4549c4a..0b202eb 100644
--- a/lib/vscode/src/vs/workbench/contrib/terminal/browser/agentHostPty.ts
+++ b/lib/vscode/src/vs/workbench/contrib/terminal/browser/agentHostPty.ts
@@ -326,6 +326,18 @@ export class AgentHostPty extends BasePty implements ITerminalChildProcess {
 		});
 	}
 
+	activateViewport(cols: number, rows: number): void {
+		if (this._inReplay) {
+			return;
+		}
+		this._startBarrier.wait().then(() => {
+			this._connection.dispatch(
+				this._terminalUri.toString(),
+				{ type: ActionType.TerminalResized, cols, rows },
+			);
+		});
+	}
+
 	shutdown(_immediate: boolean): void {
 		this._startBarrier.wait().then(() => {
 			// In attach-only mode, don't dispose the server-side terminal —
diff --git a/lib/vscode/src/vs/workbench/contrib/terminal/browser/remotePty.ts b/lib/vscode/src/vs/workbench/contrib/terminal/browser/remotePty.ts
index 68d16e8..67139e3 100644
--- a/lib/vscode/src/vs/workbench/contrib/terminal/browser/remotePty.ts
+++ b/lib/vscode/src/vs/workbench/contrib/terminal/browser/remotePty.ts
@@ -91,6 +91,15 @@ export class RemotePty extends BasePty implements ITerminalChildProcess {
 		});
 	}
 
+	activateViewport(cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): void {
+		if (this._inReplay) {
+			return;
+		}
+		this._startBarrier.wait().then(_ => {
+			this._remoteTerminalChannel.activateViewport(this.id, cols, rows, pixelWidth, pixelHeight);
+		});
+	}
+
 	async clearBuffer(): Promise<void> {
 		await this._remoteTerminalChannel.clearBuffer(this.id);
 	}
diff --git a/lib/vscode/src/vs/workbench/contrib/terminal/browser/terminalInstance.ts b/lib/vscode/src/vs/workbench/contrib/terminal/browser/terminalInstance.ts
index f4007d4..fbbc22b 100644
--- a/lib/vscode/src/vs/workbench/contrib/terminal/browser/terminalInstance.ts
+++ b/lib/vscode/src/vs/workbench/contrib/terminal/browser/terminalInstance.ts
@@ -77,6 +77,7 @@ import { getUriLabelForShell, getShellIntegrationTimeout, getWorkspaceForTermina
 import { IEditorService } from '../../../services/editor/common/editorService.js';
 import { IWorkbenchEnvironmentService } from '../../../services/environment/common/environmentService.js';
 import { IHistoryService } from '../../../services/history/common/history.js';
+import { IHostService } from '../../../services/host/browser/host.js';
 import { isHorizontal, IWorkbenchLayoutService } from '../../../services/layout/browser/layoutService.js';
 import { IPathService } from '../../../services/path/common/pathService.js';
 import { IPreferencesService } from '../../../services/preferences/common/preferences.js';
@@ -405,6 +406,7 @@ export class TerminalInstance extends Disposable implements ITerminalInstance {
 		@ICommandService private readonly _commandService: ICommandService,
 		@IAccessibilitySignalService private readonly _accessibilitySignalService: IAccessibilitySignalService,
 		@IViewDescriptorService private readonly _viewDescriptorService: IViewDescriptorService,
+		@IHostService private readonly _hostService: IHostService,
 	) {
 		super();
 
@@ -535,6 +537,11 @@ export class TerminalInstance extends Disposable implements ITerminalInstance {
 		this.statusList = this._register(this._scopedInstantiationService.createInstance(TerminalStatusList));
 		this._initDimensions();
 		this._processManager = this._createProcessManager();
+		this._register(this._hostService.onDidChangeFocus(focused => {
+			if (focused && this._isVisible) {
+				this._activateViewport();
+			}
+		}));
 
 		this._containerReadyBarrier = new AutoOpenBarrier(Constants.WaitForContainerThreshold);
 		this._attachBarrier = new AutoOpenBarrier(1000);
@@ -1201,6 +1208,7 @@ export class TerminalInstance extends Disposable implements ITerminalInstance {
 			return true;
 		});
 		this._register(dom.addDisposableListener(xterm.raw.element, 'mousedown', () => {
+			this._activateViewport();
 			// We need to listen to the mouseup event on the document since the user may release
 			// the mouse button anywhere outside of _xterm.element.
 			const listener = dom.addDisposableListener(xterm.raw.element!.ownerDocument, 'mouseup', () => {
@@ -1213,9 +1221,15 @@ export class TerminalInstance extends Disposable implements ITerminalInstance {
 		// Composery: focus on Tap rather than raw touchstart - a touch that begins a scroll
 		// of the terminal buffer must not focus the terminal and pop the on-screen keyboard.
 		this._register(Gesture.addTarget(xterm.raw.element));
+		this._register(dom.addDisposableListener(xterm.raw.element, 'touchstart', () => {
+			this._activateViewport();
+		}, { passive: true }));
 		this._register(dom.addDisposableListener(xterm.raw.element, TouchEventType.Tap, () => {
 			xterm.raw.focus();
 		}));
+		this._register(dom.addDisposableListener(xterm.raw.element, dom.EventType.MOUSE_WHEEL, () => {
+			this._activateViewport();
+		}, { passive: true }));
 
 		// xterm.js currently drops selection on keyup as we need to handle this case.
 		this._register(dom.addDisposableListener(xterm.raw.element, 'keyup', () => {
@@ -1246,6 +1260,7 @@ export class TerminalInstance extends Disposable implements ITerminalInstance {
 
 	private _setFocus(focused?: boolean): void {
 		if (focused) {
+			this._activateViewport();
 			this._terminalFocusContextKey.set(true);
 			this._setShellIntegrationContextKey();
 			this._onDidFocus.fire(this);
@@ -1441,7 +1456,11 @@ export class TerminalInstance extends Disposable implements ITerminalInstance {
 			// Resize to re-evaluate dimensions, this will ensure when switching to a terminal it is
 			// using the most up to date dimensions (eg. when terminal is created in the background
 			// using cached dimensions of a split terminal).
-			this._resize();
+			void this._resize().then(() => {
+				if (this._hostService.hasFocus) {
+					this._activateViewport();
+				}
+			});
 		}
 		if (didChange) {
 			this._onDidChangeVisibility.fire(visible);
@@ -1576,7 +1595,12 @@ export class TerminalInstance extends Disposable implements ITerminalInstance {
 		}));
 
 		this._initialDataEventsListener.value = processManager.onProcessData(ev => this._initialDataEvents?.push(ev.data));
-		this._register(processManager.onProcessReplayComplete(() => this._onProcessReplayComplete.fire()));
+		this._register(processManager.onProcessReplayComplete(() => {
+			this._onProcessReplayComplete.fire();
+			if (this._isVisible && this._hostService.hasFocus) {
+				this._activateViewport();
+			}
+		}));
 		this._register(processManager.onEnvironmentVariableInfoChanged(e => this._onEnvironmentVariableInfoChanged(e)));
 		this._register(processManager.onPtyDisconnect(() => {
 			if (this.xterm) {
@@ -2093,6 +2117,21 @@ export class TerminalInstance extends Disposable implements ITerminalInstance {
 		await this._processManager.setDimensions(rawXterm.cols, rawXterm.rows, undefined, roundedPixelWidth, roundedPixelHeight);
 	}
 
+	private _activateViewport(): void {
+		const rawXterm = this.xterm?.raw;
+		if (!this._isVisible || !rawXterm) {
+			return;
+		}
+		const pixelWidth = rawXterm.dimensions?.css.canvas.width;
+		const pixelHeight = rawXterm.dimensions?.css.canvas.height;
+		void this._processManager.activateViewport(
+			rawXterm.cols,
+			rawXterm.rows,
+			pixelWidth ? Math.round(pixelWidth) : undefined,
+			pixelHeight ? Math.round(pixelHeight) : undefined
+		);
+	}
+
 	setShellType(shellType: TerminalShellType | undefined) {
 		if (this._shellType === shellType) {
 			return;
diff --git a/lib/vscode/src/vs/workbench/contrib/terminal/browser/terminalProcessManager.ts b/lib/vscode/src/vs/workbench/contrib/terminal/browser/terminalProcessManager.ts
index 9c13e48..1652ca2 100644
--- a/lib/vscode/src/vs/workbench/contrib/terminal/browser/terminalProcessManager.ts
+++ b/lib/vscode/src/vs/workbench/contrib/terminal/browser/terminalProcessManager.ts
@@ -622,6 +622,11 @@ export class TerminalProcessManager extends Disposable implements ITerminalProce
 		return this.ptyProcessReady.then(() => this._resize(cols, rows, pixelWidth, pixelHeight));
 	}
 
+	async activateViewport(cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): Promise<void> {
+		await this.ptyProcessReady;
+		this._process?.activateViewport?.(cols, rows, pixelWidth, pixelHeight);
+	}
+
 	async setUnicodeVersion(version: '6' | '11'): Promise<void> {
 		return this._process?.setUnicodeVersion(version);
 	}
diff --git a/lib/vscode/src/vs/workbench/contrib/terminal/common/remote/remoteTerminalChannel.ts b/lib/vscode/src/vs/workbench/contrib/terminal/common/remote/remoteTerminalChannel.ts
index 666d4ff..18e106c 100644
--- a/lib/vscode/src/vs/workbench/contrib/terminal/common/remote/remoteTerminalChannel.ts
+++ b/lib/vscode/src/vs/workbench/contrib/terminal/common/remote/remoteTerminalChannel.ts
@@ -253,6 +253,9 @@ export class RemoteTerminalChannelClient implements IPtyHostController {
 	resize(id: number, cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): Promise<void> {
 		return this._channel.call(RemoteTerminalChannelRequest.Resize, [id, cols, rows, pixelWidth, pixelHeight]);
 	}
+	activateViewport(id: number, cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): Promise<void> {
+		return this._channel.call(RemoteTerminalChannelRequest.ActivateViewport, [id, cols, rows, pixelWidth, pixelHeight]);
+	}
 	clearBuffer(id: number): Promise<void> {
 		return this._channel.call(RemoteTerminalChannelRequest.ClearBuffer, [id]);
 	}
diff --git a/lib/vscode/src/vs/workbench/contrib/terminal/common/remote/terminal.ts b/lib/vscode/src/vs/workbench/contrib/terminal/common/remote/terminal.ts
index 1344c39..4ac600d 100644
--- a/lib/vscode/src/vs/workbench/contrib/terminal/common/remote/terminal.ts
+++ b/lib/vscode/src/vs/workbench/contrib/terminal/common/remote/terminal.ts
@@ -74,6 +74,7 @@ export const enum RemoteTerminalChannelRequest {
 	AcknowledgeDataEvent = '$acknowledgeDataEvent',
 	Shutdown = '$shutdown',
 	Resize = '$resize',
+	ActivateViewport = '$activateViewport',
 	ClearBuffer = '$clearBuffer',
 	GetInitialCwd = '$getInitialCwd',
 	GetCwd = '$getCwd',
diff --git a/lib/vscode/src/vs/workbench/contrib/terminal/common/terminal.ts b/lib/vscode/src/vs/workbench/contrib/terminal/common/terminal.ts
index f1bcb84..73cdc10 100644
--- a/lib/vscode/src/vs/workbench/contrib/terminal/common/terminal.ts
+++ b/lib/vscode/src/vs/workbench/contrib/terminal/common/terminal.ts
@@ -307,6 +307,7 @@ export interface ITerminalProcessManager extends IDisposable, ITerminalProcessIn
 	setDimensions(cols: number, rows: number, sync?: undefined, pixelWidth?: number, pixelHeight?: number): Promise<void>;
 	setDimensions(cols: number, rows: number, sync: false, pixelWidth?: number, pixelHeight?: number): Promise<void>;
 	setDimensions(cols: number, rows: number, sync: true, pixelWidth?: number, pixelHeight?: number): void;
+	activateViewport(cols: number, rows: number, pixelWidth?: number, pixelHeight?: number): Promise<void>;
 	clearBuffer(): Promise<void>;
 	setUnicodeVersion(version: '6' | '11'): Promise<void>;
 	setNextCommandId(commandLine: string, commandId: string): Promise<void>;
