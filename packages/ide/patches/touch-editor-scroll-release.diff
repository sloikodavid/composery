diff --git a/lib/vscode/src/vs/editor/browser/controller/pointerHandler.ts b/lib/vscode/src/vs/editor/browser/controller/pointerHandler.ts
--- a/lib/vscode/src/vs/editor/browser/controller/pointerHandler.ts
+++ b/lib/vscode/src/vs/editor/browser/controller/pointerHandler.ts
@@ -264,6 +264,12 @@ export class PointerEventHandler extends MouseHandler {
 				}
 			}
 
+			if (this._touchGesture) {
+				// Non-text editor chrome scrolls immediately rather than waiting for the selection
+				// threshold. Once scrolling is applied, release must not also focus as a tap.
+				this._touchGesture.panned = true;
+				this._clearTouchSelectionTimer();
+			}
 			this._context.viewModel.viewLayout.deltaScrollNow(-event.translationX, -event.translationY);
 		}
 		if (this._lastPointerType === 'pen') {
